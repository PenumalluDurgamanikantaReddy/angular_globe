// Lightweight Google Maps typings placeholder for build-time only.
// For better typing, install `@types/googlemaps` and adjust tsconfig.
declare const google: any;
declare namespace google {
  const maps: any;
}
